// clangd requires the file field in compile_commands.json to be set to the
// absolute path of an existing file, so bear uses this file to generate that
// every time shell.nix is loaded
int main() {}
